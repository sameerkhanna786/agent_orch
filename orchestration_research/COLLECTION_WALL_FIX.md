{
  "summary": "Adversarially validate the optimal AST collection-wall detection algorithm (submodule-aware resolution + conftest-gated Phase-0 trigger) against the 4 eval repo bases + Python import edge cases, before implementing",
  "agentCount": 6,
  "logs": [],
  "result": {
    "probes": [
      {
        "repo": "babel",
        "checkout_found": true,
        "has_conftest": true,
        "conftest_unresolved_symbols": [],
        "testfile_unresolved_count": 0,
        "submodule_false_positives": 18,
        "genuine_conftest_wall": false,
        "verdict": "phase0_should_NOT_fire",
        "evidence": [
          "Checkout: /tmp/omega_diag_ab/hybrid-nogate__babel__s1/worktrees/wt_a812000 (package root; the babel/ package lives inside).",
          "Two conftest.py files: /conftest.py and /tests/conftest.py. Top-level imports are stdlib/pytest only (pathlib, _pytest.doctest, os, pytest, zoneinfo, pytz). No top-level repo-internal import.",
          "The ONLY repo-internal reference in any conftest is `from babel.localedata import locale_identifiers` at tests/conftest.py:30, and it is DEFERRED inside pytest_generate_tests (not collection-time). babel/localedata.py exists and defines locale_identifiers (line 108) -> resolves.",
          "Detector would flag 18 `from PKG import NAME` package-imports as missing_symbol (NAME not in PKG/__init__). ALL 18 are existing submodule files: babel/{core,dates,lists,localedata,numbers,plural,support,units,util}.py and babel/messages/{catalog,checkers,extract,frontend,jslexer,mofile,plurals,pofile,setuptools_frontend}.py -> 18/18 false positives fixed by submodule-aware resolution.",
          "The other 35 imports are `from <leaf module> import <symbol>` (e.g. babel.core.Locale, babel.messages.pofile.read_po); all 35 resolve to real top-level defs/classes in those leaf .py modules (0 unresolved).",
          "GENUINE conftest wall = NONE: no conftest imports a name that is neither in the target __init__ nor an existing submodule (not the pydantic GenerateSchema case).",
          "Live sanity check in the checkout: `import babel, babel.messages, babel.localedata; from babel.localedata import locale_identifiers` succeeds, 1013 locales loaded -> repo collects/imports cleanly and decomposes+solves normally.",
          "Conclusion: with submodule-aware resolution + conftest-gated trigger, Phase 0 (synthetic make-it-collect) should NOT fire for babel."
        ]
      },
      {
        "repo": "mimesis",
        "checkout_found": true,
        "has_conftest": true,
        "conftest_unresolved_symbols": [],
        "testfile_unresolved_count": 3,
        "submodule_false_positives": 3,
        "genuine_conftest_wall": false,
        "verdict": "phase0_should_NOT_fire",
        "evidence": [
          "Checkout/package root: /tmp/omega_diag_ab/hybrid-diag__mimesis__s1/worktrees/wt_a0 (mimesis/ package inside).",
          "Exactly one conftest: tests/conftest.py. Its repo-internal imports are only `import mimesis` and `from mimesis.locales import Locale` — both resolve structurally (locales.py is an existing submodule; Locale is importable from it). Fixtures use attribute access (mimesis.Generic/Address/Finance/Datetime/Food/Person/Text/Path) — all of which are top-level names in mimesis/__init__.py.",
          "Strict genuine_conftest_wall test = NONE: no conftest `from mimesis import NAME` where NAME is neither in __init__ nor an existing submodule.",
          "mimesis/__init__.py binds 67 top-level names (19 of the 22 distinct `from mimesis import NAME` test imports resolve there).",
          "Detector-flagged `from mimesis import NAME` not in __init__: datasets, random, shortcuts — ALL 3 are real submodule files on disk (mimesis/datasets/__init__.py exists; mimesis/random.py exists; mimesis/shortcuts.py exists). => 3 submodule false positives, 0 genuinely missing.",
          "Actual collection error is `ImportError: cannot import name 'validate_locale' from 'mimesis.locales'`, raised in mimesis/keys.py line 9 via `from mimesis.locales import Locale, validate_locale`. This is a SUBMODULE-INTERNAL `from PKG.SUBMODULE import NAME`, NOT a top-level `from PKG import NAME`, and validate_locale is a stripped-out FUNCTION the agent must implement (declared in locales.py __all__, tested by tests/test_locales.py). It is normal commit0 implement-from-tests work, not a structural symbol/module wall like pydantic GenerateSchema.",
          "validate_locale is never imported via `from mimesis import ...` in conftest or __init__; it only appears as `from mimesis.locales import validate_locale` in tests/test_locales.py.",
          "Conclusion: mimesis is in the babel/networkx decompose+solve class; submodule-aware resolution removes the 3 false positives and the conftest-gated trigger finds no genuine wall, so a synthetic make-it-collect Phase 0 should NOT fire."
        ]
      },
      {
        "repo": "pydantic",
        "checkout_found": true,
        "has_conftest": true,
        "conftest_unresolved_symbols": [
          "GenerateSchema"
        ],
        "genuine_conftest_wall": true,
        "testfile_unresolved_count": 122,
        "submodule_false_positives": 4,
        "verdict": "phase0_should_fire",
        "evidence": [
          "Checkout: /tmp/omega_diag_ab/hybrid-nogate__pydantic__s1/worktrees/wt_a0 (package root = .../pydantic). This is a commit0 implement-from-tests base: function/class bodies stripped to `pass`+docstring (e.g. pydantic/main.py BaseModel.__init__ body is `pass`; pydantic/_internal/_generate_schema.py methods are `pass`), but module-level machinery in __init__.py is intact.",
          "Exactly ONE conftest: /tmp/omega_diag_ab/hybrid-nogate__pydantic__s1/worktrees/wt_a0/tests/conftest.py. Its only repo-internal top-level-name import is line 17: `from pydantic import GenerateSchema`. This runs unconditionally at collection time for the whole session, so it is the true collection gate.",
          "GenerateSchema is NOT a static (non-TYPE_CHECKING) top-level name in pydantic/__init__.py. AST of the body excluding the TYPE_CHECKING block yields only: VERSION, __all__, __dir__, __getattr__, __version__, _dynamic_imports, _getattr_migration, getattr_migration, typing. GenerateSchema appears ONLY under `if TYPE_CHECKING:` (line 19 `from ._internal._generate_schema import GenerateSchema as GenerateSchema`), in __all__ (line 217), and in _dynamic_imports (line 370), resolved at runtime via __getattr__ -> import_module('._internal._generate_schema').",
          "GenerateSchema is NOT a submodule: pydantic/GenerateSchema.py does NOT exist and pydantic/GenerateSchema/__init__.py does NOT exist (verified with ls). So submodule-aware resolution does NOT fix it -> it is the GENUINE pydantic-GenerateSchema conftest wall the task references.",
          "A static, submodule-aware, import-time detector cannot resolve GenerateSchema at conftest collection because resolution requires executing __getattr__ at runtime. Hence collection is genuinely blocked at the conftest layer for this base -> conftest-gated trigger fires.",
          "Test-bootstrap `from pydantic import NAME` imports across tests/: 128 distinct names. Under strict-static detection (excluding TYPE_CHECKING) 126 would be flagged missing_symbol; only 4 of those are actually existing submodule files (false positives a submodule-aware fix would resolve): Json (pydantic/Json.py, 138B stub), dataclasses (pydantic/dataclasses.py), errors (pydantic/errors.py), fields (pydantic/fields.py). The remaining ~122 are not in __init__ statically and are not submodules — they resolve only at runtime via __getattr__/_dynamic_imports, so they are NOT collection walls (they only fail per-test-file at runtime, not at session-wide collection).",
          "Decisive: unlike babel/mimesis/networkx (which decompose+solve normally with no conftest collection wall), pydantic has a conftest-level import of a name that is neither in the target __init__ nor an existing submodule. Under the FIXED algorithm (submodule-aware resolution + conftest-gated trigger), the false positives are filtered out but the genuine conftest wall remains -> a synthetic make-it-collect Phase 0 SHOULD fire for pydantic."
        ]
      },
      {
        "repo": "networkx",
        "checkout_found": true,
        "has_conftest": true,
        "conftest_unresolved_symbols": [],
        "genuine_conftest_wall": false,
        "submodule_false_positives": 0,
        "testfile_unresolved_count": 51,
        "verdict": "phase0_should_NOT_fire",
        "evidence": [
          "Checkout package root: /tmp/omega_diag_ab/hybrid-diag__networkx__s0/cells/autogen_orchestrator__networkx/repo/networkx (also present as worktrees/wt_a0). Pristine commit0 base at git HEAD ec23f3a 'apex: scrub upstream version identifiers'.",
          "Exactly ONE conftest.py exists repo-wide: networkx/conftest.py. No conftest.py in any subdir, doc/, or examples/.",
          "networkx/conftest.py has ZERO 'from networkx import NAME' statements. Its only repo-internal import is line 21 'import networkx' (plain whole-package import). All other imports are stdlib (os, sys, warnings, importlib.metadata.entry_points) or optional deps (numpy, scipy, matplotlib, pandas, pygraphviz, pydot, sympy) inside try/except.",
          "Conftest references networkx.utils.backends, networkx.config, networkx.utils.backends._dispatchable only as runtime attribute accesses inside pytest hooks/fixtures, not as bootstrap symbol imports — nothing for the missing-symbol detector to flag at collection bootstrap.",
          "AST scan of __init__.py: static top-level names are only ['__version__','_clear_cache','_dispatchable','_lazy_import','algorithms','classes','config','convert','convert_matrix','drawing','filters','generators','linalg','readwrite','relabel','utils'] PLUS 10 star re-exports: from networkx.{exception,classes,convert,convert_matrix,relabel,generators,readwrite,algorithms,linalg,drawing} import *.",
          "Across the whole repo there are 65 'from networkx import NAME' (exact module) statements; 51 reference names absent from __init__ static top-level names. SUBMODULE-rescuable among them: 0. All 51 are star-reexported runtime symbols (e.g. NetworkXError, Graph, DiGraph, MultiGraph, PlanarEmbedding, convert_node_labels_to_integers, vf2pp_is_isomorphic), located in test/module files, NOT in conftest.",
          "submodule_false_positives = 0: the specific submodule-resolution bug (PKG/NAME.py or PKG/NAME/__init__.py exists) produces no false positives for this repo because none of the flagged names are submodules.",
          "No GENUINE conftest wall: there is no conftest import of a name that is neither in __init__ nor an existing submodule (no pydantic-GenerateSchema analogue).",
          "Base currently fails to import only because bodies are gutted to stubs — e.g. networkx/lazy_imports.py line 51 'class DelayedImportErrorModule(types.ModuleType):' has an empty body (IndentationError). This is a syntax/stub gap that decompose+solve fills in normally, NOT a missing-symbol/conftest collection wall.",
          "networkx matches the babel/mimesis profile: collects-and-solves normally once stubs are implemented; conftest-gated trigger sees no unresolved conftest symbol, so a synthetic make-it-collect Phase 0 should stay OFF."
        ]
      }
    ],
    "edge_cases": {
      "edge_cases": [
        {
          "name": "(a) from pkg import submodule — pkg/submodule.py exists",
          "example": "conftest.py: `from pkg import support` where pkg/support.py exists but pkg/__init__.py is stripped (no re-export of `support`).",
          "correct_behavior": "RESOLVES. `from pkg import NAME` is valid whenever NAME is a submodule FILE (pkg/NAME.py) regardless of whether NAME appears in pkg/__init__.py. The detector must check `idx.resolve(f\"{mod}.{name}\")` BEFORE consulting __init__'s top-level names. Implemented correctly at diagnose_ast.py:263 — the submodule-aware branch returns/continues without flagging, and bumps import_depth to depth+1. Verified: test_submodule_import_not_flagged_as_missing_symbol passes, unresolved_internal == [].",
          "risk_if_mishandled": "FALSE-POSITIVE collection wall. This is the documented babel-922 regression: all 18 'missing' symbols were existing submodule files; flagging them as missing_symbol fired a wrong Phase 0 and mis-steered the run to 922/5663 vs the baseline's full 5663 solve. A commit0 base strips __init__ re-exports but KEEPS the submodule files, so every `from pkg import submodule` would falsely read as a wall without this check."
        },
        {
          "name": "(b) from pkg import subpkg — pkg/subpkg/__init__.py exists",
          "example": "`from pkg import sub` where pkg/sub/__init__.py exists (subpackage), pkg/__init__.py has no re-export of `sub`.",
          "correct_behavior": "RESOLVES. Identical to (a): `idx.resolve('pkg.sub')` returns the subpackage's __init__.py (resolve() falls back to root/pkg/sub/__init__.py at diagnose_ast.py:96-98), so the submodule-aware branch at :263 treats it as a valid deeper internal module and continues. Verified in test_submodule_import_not_flagged_as_missing_symbol: the `from pkg import sub` line with pkg/sub/__init__.py present is not flagged.",
          "risk_if_mishandled": "FALSE-POSITIVE collection wall — same babel-922 class as (a). A subpackage import that is mistaken for a missing __init__ symbol would put a real, present package into must_implement_modules and trigger an unnecessary Phase 0."
        },
        {
          "name": "(c) from pkg import name — re-export in pkg/__init__",
          "example": "`from pkg import Thing` where pkg/__init__.py contains `from pkg.core import Thing` (a re-export), and pkg has no Thing.py submodule.",
          "correct_behavior": "RESOLVES. Not a submodule, so it falls through to the symbol check: `_module_top_level_names(pkg/__init__.py)` collects the re-exported name (the ImportFrom alias `Thing` is added by the Import/ImportFrom branch at diagnose_ast.py:66-68). `name in names` is True ⇒ not flagged. Verified: test_clean_repo_collects_cleanly passes with `from pkg import Thing` resolving through the __init__ re-export.",
          "risk_if_mishandled": "FALSE-POSITIVE wall if __init__ re-exports were not parsed as exported names — the detector would think the symbol is absent. Note the genuine residual gap: this is STATIC name presence only. It does NOT follow the re-export to verify pkg.core.Thing actually exists. If __init__ re-exports `Thing` but pkg/core.py is stripped, the static check passes (Thing IS named in __init__) while runtime collection would still fail — a FALSE-NEGATIVE (missed wall). Acceptable conservative bias, but worth noting."
        },
        {
          "name": "(d) import a.b.c (dotted plain import)",
          "example": "`import pkg.a.b` in conftest, with pkg/a/b.py present (or absent).",
          "correct_behavior": "RESOLVES when the deepest dotted module exists. `_iter_imports` yields (module='pkg.a.b', name=None, depth=3) at diagnose_ast.py:111-114. `idx.resolve('pkg.a.b')` is checked; if present, internal-resolved and import_depth=max(.,3). If pkg/a/b.py is missing, recorded as unresolved_internal reason=missing_module. Verified: test_import_depth_rises_with_resolvable_chain gives import_depth>=3. Note: Python binds `import a.b.c` to the top name `a` and requires every intermediate package to import successfully; the detector only verifies the leaf module file exists, which is the right collection-time signal here.",
          "risk_if_mishandled": "Mild FALSE-NEGATIVE risk: if only the leaf exists but an intermediate package (a/__init__.py) raises at import, static file-presence won't catch it. Conversely, if depth were taken from the wrong node, import_depth (the SPFG++ rising signal) would be wrong, distorting plateau-vs-progress governance even when collection is fine."
        },
        {
          "name": "(e) relative imports — from . import x / from ..y import z",
          "example": "pkg/conftest.py: `from .core import Thing` (level=1) or `from ..util import z` (level=2).",
          "correct_behavior": "CURRENTLY SKIPPED ENTIRELY. `_iter_imports` does `if node.level and node.level > 0: continue` (diagnose_ast.py:117) — relative imports are never classified, so they never contribute to unresolved_internal and never flip collects_cleanly. Verified: the relative-import-in-conftest probe returned collects_cleanly=True with empty unresolved_internal even though .core.Thing was stripped. The comment justifies this as 'resolved within its own package, skip cross-repo classify.'",
          "risk_if_mishandled": "FALSE-NEGATIVE (missed wall). A conftest.py using `from .core import GenerateSchema` against a stripped pkg/core.py is a genuine total collection wall, but the current skip means it is NOT detected and no Phase 0 fires. The correct fix is to resolve relative imports against the importer's own package path (anchor = importer dir, climb `level-1` parents, then join node.module) and classify the result like an absolute internal import — relative imports are ALWAYS internal, so skipping them is the most consequential blind spot for conftest-anchored walls."
        },
        {
          "name": "(f) src-layout — package under src/",
          "example": "src/pkg/schema.py present, conftest.py: `from pkg.schema import GenerateSchema` (the installed dist exposes `pkg`, but on disk it lives at src/pkg/).",
          "correct_behavior": "SHOULD resolve `pkg.schema` to src/pkg/schema.py and detect the missing GenerateSchema as a real wall. CURRENTLY BROKEN: _RepoIndex keys modules by path relative to repo root, so the index contains 'src.pkg.schema', and top_packages = {'src'}. `idx.resolve('pkg.schema')` returns None and 'pkg' is not in top_packages ⇒ the import is misclassified as EXTERNAL. Verified: the src-layout real-wall probe returned collects_cleanly=True with `pkg` parked in unresolved_external and unresolved_internal empty. The fix is to index src/ (and other source roots) as additional package roots — strip a leading 'src/' segment (or any dir on the configured sys.path/packages root) when forming the dotted key, so src/pkg/schema.py also registers as 'pkg.schema'.",
          "risk_if_mishandled": "FALSE-NEGATIVE (missed wall) — the more dangerous direction here. A real conftest collection wall in a src-layout repo is silently missed: collects_cleanly stays True, the must-implement closure is empty, no Phase 0 fires, and the run plateaus at 0/0 with no rising import_depth. (Symmetric latent risk: if src-rooted internals were ever matched against a same-named installed dist they could be mislabeled internal-resolved, a false-negative on a genuinely-stripped symbol.)"
        },
        {
          "name": "(g) PEP-420 namespace packages — no __init__.py",
          "example": "pkg/core.py present with NO pkg/__init__.py. Two sub-cases: (g1) `from pkg.core import Thing`; (g2) `from pkg import core`.",
          "correct_behavior": "Both forms are valid at runtime under PEP-420 and must RESOLVE. (g1) WORKS today: by_dotted contains 'pkg.core' (keyed straight off the file path, no __init__ required), so resolve('pkg.core') hits and top_packages includes 'pkg' (derived from by_dotted, not from __init__ presence). Verified collects_cleanly=True. (g2) BROKEN: resolve('pkg') looks only for pkg in by_dotted or pkg/__init__.py — neither exists for a pure namespace dir — so `from pkg import core` is recorded unresolved_internal reason=missing_module. Verified: the from-import-submodule-under-namespace probe returned collects_cleanly=False, a spurious wall. Fix: in resolve(), also treat a directory that contains any indexed *.py (a namespace package dir) as resolvable, and in the (a)/(b) submodule check, recognize pkg/core.py as a submodule of the namespace pkg.",
          "risk_if_mishandled": "(g2) is a FALSE-POSITIVE collection wall: a perfectly importable namespace-package submodule is flagged missing_module and, if in conftest, fires a wrong Phase 0 / pollutes must_implement_modules. (g1) is fine but relies on the file-path indexing — if resolve() ever required __init__.py for the base package, all namespace packages would FALSE-POSITIVE."
        },
        {
          "name": "(h) from pkg import * (wildcard)",
          "example": "conftest.py: `from pkg import *`; separately, a module whose __init__ does `from .core import *`.",
          "correct_behavior": "TREAT AS PASSTHROUGH / do not flag. At the import site, _iter_imports maps the `*` alias to name=None (diagnose_ast.py:123), so it is checked only as a module-existence import (does pkg resolve?) and never as a missing symbol. At the exported-names side, a module containing `from x import *` adds the sentinel '*' to its name set (diagnose_ast.py:70-72), and the symbol check at :267 short-circuits on `'*' in names` so NO downstream `from thatmod import anything` is ever flagged missing. This is the correct conservative choice: a wildcard re-exports an unknowable set.",
          "risk_if_mishandled": "Wrong direction either way. If `*` were treated as a literal symbol name, every wildcard import would FALSE-POSITIVE (no module exports a name called '*'). If the '*' sentinel on the exporting side were dropped, names that are actually pulled in via a wildcard re-export would be FALSE-POSITIVE-flagged as missing. The deliberate bias is false-negative (let wildcard-fed names through) because the dynamic set is unknowable statically."
        },
        {
          "name": "(i) conditional / inside-function imports",
          "example": "`try: from pkg.fast import X\\nexcept ImportError: from pkg.slow import X`, or an `import heavy` placed inside a test function / fixture body.",
          "correct_behavior": "Should NOT count toward the COLLECTION wall. Conceptually only MODULE-TOP-LEVEL imports execute at collection import time; an import guarded by try/except or nested in a function body does not crash collection. The current detector uses ast.walk (diagnose_ast.py:111) which flattens ALL imports regardless of nesting/guarding, so it over-collects. This is tolerable ONLY because the wall verdict is gated to CONFTEST top-level effect and because try/except-guarded imports are designed to not raise. The principled behavior: classify an import as collection-relevant only if it is at module top level (node in tree.body) and not inside a try whose handler catches ImportError/ModuleNotFoundError. A nested/guarded missing import is incremental work, not a wall.",
          "risk_if_mishandled": "FALSE-POSITIVE wall if a try/except-guarded fallback import (whose whole point is to tolerate absence) is flagged as a hard missing import and fires Phase 0. Conversely, counting inside-function imports as collection blockers would mark deferred/lazy imports as walls when they never run at collection time. The safe bias is to EXCLUDE conditional/nested imports from the collection-wall decision while optionally still recording them informationally for blocker_class."
        },
        {
          "name": "(j) genuinely missing symbol — the real wall",
          "example": "conftest.py: `from pkg.schema import GenerateSchema` where pkg/schema.py exists but is stripped and exports no GenerateSchema, AND no pkg/schema/GenerateSchema submodule exists (the canonical pydantic GenerateSchema collection-collapse).",
          "correct_behavior": "MUST FLAG as a collection wall. The module resolves (idx.resolve('pkg.schema') hits) but: (1) it is NOT a submodule (idx.resolve('pkg.schema.GenerateSchema') is None at :263), and (2) _module_top_level_names has no '*' and GenerateSchema not in names (:267) ⇒ recorded unresolved_internal reason=missing_symbol. Because the importer is conftest.py, conftest_unresolved is non-empty ⇒ collects_cleanly=False, first_failing_import set to the shallowest, and pkg.schema enters must_implement_modules. Verified: test_missing_symbol_in_conftest_is_collection_collapse and test_conftest_unresolved_is_a_collection_wall both assert exactly this. CONTRAST: the identical missing symbol imported only by a TEST FILE (not conftest) is incremental — collects_cleanly stays True (test_testfile_unresolved_is_not_a_collection_wall), because that file alone fails to collect while the rest of the suite runs and the module lands incrementally.",
          "risk_if_mishandled": "Two opposite failures. FALSE-NEGATIVE: missing the genuine conftest wall leaves the north-star stuck at 0/0 with no rising import_depth and no Phase 0 — the exact pathology this pre-pass exists to break. FALSE-POSITIVE: flagging a test-FILE-level missing symbol as a whole-suite wall fires a wrong Phase 0 and mis-steers the run (the babel mis-steer). The conftest-gating of the wall verdict (diagnose_ast.py:284-301) is the load-bearing distinction that keeps both errors at bay."
        }
      ]
    },
    "spec": "I now have full grounding: the current implementation, the trigger, and the existing tests. The probes confirm the current code already handles babel/mimesis/pydantic/networkx correctly (submodule-aware + conftest-gated). The edge cases identify the remaining gaps: (e) relative imports skipped, (f) src-layout broken, (g2) namespace `from pkg import sub` broken, plus (i) nested/guarded imports over-collected. Here is the synthesized spec.\n\n---\n\n# SPEC: Static Collection-Wall Detection (apex_omega/autogen/diagnose_ast.py)\n\n## Status of current code vs. the four probes\nThe current implementation (submodule-aware resolve at `:263` + conftest-gating at `:284-287`) **already produces the correct verdict for all four probed repos**. The spec below (1) pins those rules precisely and (2) closes the four remaining edge-case gaps that no probed repo exercised but that will mis-fire on other commit0 bases.\n\n---\n\n## (1) Resolution rule for `from MOD import NAME`\n\nGiven importer file `F`, import `from MOD import NAME` (absolute; `level==0`), index `idx`:\n\n1. **Classify internal vs external.** `top = MOD.split(\".\")[0]`. Internal iff `top in idx.top_packages OR idx.resolve(MOD) is not None`. Else external — record `top` in `unresolved_external` only if `not _external_importable(top)`; never a wall. (unchanged, `:237-243`)\n2. **Module existence.** `target = idx.resolve(MOD)`. If `None` → `unresolved_internal` reason=`missing_module`. (unchanged, `:244-252`)\n3. **Submodule-FIRST symbol check** (the load-bearing rule; covers edge (a)(b)(g2)):\n   `if idx.resolve(f\"{MOD}.{NAME}\") is not None: continue` (it's a submodule/subpackage file — valid regardless of `__init__` contents; bump `import_depth` to `depth+1`). This MUST run **before** consulting `__init__` names. (current `:263`)\n4. **Top-level-name check.** `names = _module_top_level_names(target)`. If `names is not None AND \"*\" not in names AND NAME not in names` → `unresolved_internal` reason=`missing_symbol`. `\"*\"` sentinel = wildcard re-export passthrough (edge (h), `:267-273`). `NAME is None` (plain `import`/`*`) → module-existence only, never a symbol miss (edge (h) site, `:255`).\n\nThis rule resolves edges (a)(b)(c)(d)(g1)(h) and correctly flags (j). The submodule-first ordering is what kills the babel-18/pydantic-4 false positives.\n\n## (2) Classify-by-importer → what drives `collects_cleanly` + the must-implement closure\n\n- Record **every** unresolved internal import (conftest AND test-file) in `unresolved_internal` (informational, feeds `blocker_class`).\n- **`conftest_unresolved`** = subset where `Path(importer).name == \"conftest.py\"`. (`:284-285`)\n- **`collects_cleanly = not conftest_unresolved`** — conftest imports run session-wide at collection, so an unresolved one walls the whole suite; a test-file unresolved import only fails that one file (rest collects + solves incrementally). (`:287`)\n- **`must_implement_modules`** = dedup'd `module` values from `conftest_unresolved` only. (`:288-294`)\n- **`first_failing_import`** = shallowest (fewest dots) conftest blocker. (`:296`)\n- **Phase-0 trigger (architect.py `:606-607`)**: fire iff `APEX_OMEGA_PHASE0` enabled AND `diag.collects_cleanly is False` — i.e. ≥1 genuine conftest wall. **No change needed.** This is exactly \"fire on pydantic GenerateSchema, not on babel test-file submodules.\"\n\n## Required fixes (edge cases not covered by current code)\n\n- **(e) Relative imports — MUST FIX (highest-value blind spot).** Replace the `if node.level: continue` skip at `:116-117`. Resolve against importer's package: `anchor = F.parent`; climb `level-1` parents; prepend the resulting dotted package to `node.module`; classify the absolute result via rule (1). Relative imports are *always* internal. A `conftest.py: from .core import GenerateSchema` against a stripped `core.py` is a genuine wall currently missed entirely (false-negative).\n- **(f) src-layout — MUST FIX.** In `_RepoIndex.__init__`, when forming the dotted key, strip a leading source-root segment (`src/`, and any dir listed in `[tool.setuptools.packages.find] where` / `package_dir`) so `src/pkg/schema.py` registers as `pkg.schema` (not `src.pkg.schema`). Otherwise real walls in src-layout repos are silently missed (false-negative; the more dangerous direction).\n- **(g2) namespace `from pkg import sub`, no `pkg/__init__.py` — FIX in `resolve()`.** Treat a directory containing any indexed `*.py` as a resolvable namespace package: `resolve(\"pkg\")` should hit when `pkg/` holds indexed modules even without `__init__.py`. Prevents a spurious `missing_module` wall on PEP-420 packages. (g1 already works.)\n- **(i) nested/guarded imports — SHOULD FIX (precision).** Switch from `ast.walk` (`:111`, flattens all) to: collection-relevant iff the import node is in `tree.body` (module top level) AND not inside a `try` whose handler catches `ImportError`/`ModuleNotFoundError`. Nested/guarded imports → record informationally only, never count toward `collects_cleanly`. Prevents a try/except fallback import from firing a wrong Phase 0.\n\n## (3) Additional wall signal: high-fraction shared missing symbol\n\n**Recommendation: compute it, surface it, but do NOT let it drive `collects_cleanly` or Phase-0 now.**\n- Add `dominant_testfile_missing`: from test-file (non-conftest) `unresolved_internal`, the `(module,symbol)` whose missing-fraction across distinct bootstrap test files exceeds a threshold (e.g. ≥0.6). Emit as advisory evidence + a `blocker_class` hint.\n- **Do not gate on it.** It is genuinely *not* a total collection wall (each file still collects independently as the symbol lands), and `_synthesize_phase0` already falls back to the whole gold set when no conftest closure exists, so the steering benefit is marginal. The conftest gate is the principled total-wall signal; adding a fractional gate re-introduces exactly the babel mis-steer risk (false-positive Phase 0). Not worth the complexity now — ship as advisory metadata only.\n\n## (4) Expected verdict per repo (after fixes; unchanged from current correct behavior)\n\n| repo | conftest wall? | `collects_cleanly` | Phase-0 fires? |\n|---|---|---|---|\n| **babel** | none (18 submodule FPs resolved; the 1 conftest ref is deferred + resolves) | **True** | **NO** |\n| **mimesis** | none (3 submodule FPs resolved; `validate_locale` is a test-file `from pkg.SUB import` runtime miss, not conftest) | **True** | **NO** |\n| **pydantic** | YES — `conftest.py: from pydantic import GenerateSchema`; not in static `__init__` (TYPE_CHECKING-only), not a submodule | **False** | **YES** |\n| **networkx** | none (0 submodule FPs; conftest has no `from networkx import NAME`; failure is stub IndentationError = decompose+solve) | **True** | **NO** |\n\n## (5) Test cases to add (`tests/test_diagnose_ast.py`)\n\nExisting tests already cover (a)/(b) submodule, (c) re-export, (d) depth, (h) external, (j) conftest-vs-testfile wall, never-raises. Add:\n\n1. **`test_relative_import_in_conftest_is_a_wall`** (edge e) — `conftest.py: from .core import GenerateSchema`, `core.py` stripped → `collects_cleanly False`, `pkg.core` (or resolved abs) in `must_implement_modules`. (Currently FAILS — proves the e fix.)\n2. **`test_relative_import_resolves_when_symbol_present`** (edge e, negative) — same but `core.py` defines `GenerateSchema` → `collects_cleanly True`, `unresolved_internal == []`.\n3. **`test_src_layout_conftest_wall_detected`** (edge f) — `src/pkg/schema.py` stripped, `conftest.py: from pkg.schema import GenerateSchema` → `collects_cleanly False`, `pkg.schema` in `must_implement_modules`, `pkg` NOT in `unresolved_external`.\n4. **`test_src_layout_clean_collects`** (edge f, negative) — `src/pkg/schema.py` exports `GenerateSchema`, conftest imports it → `collects_cleanly True`.\n5. **`test_namespace_from_pkg_import_submodule`** (edge g2) — `pkg/core.py` present, NO `pkg/__init__.py`, `conftest.py: from pkg import core` → `collects_cleanly True`, `unresolved_internal == []`.\n6. **`test_namespace_from_pkg_submodule_import_symbol`** (edge g1, regression-pin) — no `__init__.py`, `from pkg.core import Thing` with `Thing` present → `collects_cleanly True`.\n7. **`test_try_except_guarded_import_not_a_wall`** (edge i) — `conftest.py: try: from pkg.fast import X\\nexcept ImportError: from pkg.slow import X` with `pkg.fast` missing but `pkg.slow.X` present → `collects_cleanly True`; the guarded miss is not in `conftest_unresolved`.\n8. **`test_inside_function_import_not_a_wall`** (edge i) — missing `from pkg.x import Y` inside a fixture/function body in conftest → `collects_cleanly True`.\n9. **`test_dominant_testfile_missing_symbol_is_advisory_only`** (signal 3) — same missing test-file symbol across ≥3 test files → `collects_cleanly True`, `must_implement_modules == []`, but `dominant_testfile_missing` populated (if implemented).\n10. **Regression-pin verdict tests** (probe parity) — four minimal fixtures mirroring babel (submodule FPs in test files + deferred conftest ref → True), mimesis (`from pkg.SUB import missing_fn` in test file only → True), pydantic (TYPE_CHECKING-only `__init__` name imported by conftest → False), networkx (conftest `import pkg` only, gutted-stub body → True).\n\n**Key file paths:** implementation `/Users/sameertkhanna/Documents/agent_orch/apex_omega/autogen/diagnose_ast.py` (resolution `:236-274`, conftest-gate `:284-301`, `must_implement_modules` `:319-334`); trigger `/Users/sameertkhanna/Documents/agent_orch/apex_omega/autogen/architect.py:606-607` (no change) and `_synthesize_phase0` `:541-568`; tests `/Users/sameertkhanna/Documents/agent_orch/tests/test_diagnose_ast.py`."
  }
}